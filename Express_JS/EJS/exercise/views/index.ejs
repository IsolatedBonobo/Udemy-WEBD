<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Imrpoving</title>

</head>
<body>
    <h1> <%= title %></h1>
    <p>Current second <%= second %></p>
    <% if(second % 2 === 0){ %>
        <ul>
        <% items.forEach((fruit) => { %>
                <li> <%= fruit %></li>
        <% }) %>
    </ul>
    <% }else { %>
        <p>No items to be display</p>
        <% } %>
    <p><%- htmlcontent %></p>
    <%- include("footer.ejs") %>
</body>
</html>